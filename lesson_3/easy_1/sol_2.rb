# 1. use != to test difference

# 2. put ! before something: !user_name: if user_name is true then !user_name will be false and so

# 3. put ! after something: arr.uniq!: usually means that it mutates the caller

# 4. put ? before something: ternary oparator

# 5. put ? after something: is_integer?(number): return true if number is an integer

# 6. put !! before something: !!user_name: the boolean value of user_name